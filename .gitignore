# Python
__pycache__/
*.pyc
*.pyo

# Virtual environment (only needed locally)
stock_sim_env/
venv/
.venv/

# Trade logs — ignore root-level file, but allow committed logs/ directory
trade_log*.csv
!logs/
!logs/*.csv

# Bankroll carry-over state (committed to repo so CI can read it next session)
!bankroll.json

# OS
.DS_Store
Thumbs.db
